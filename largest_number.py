# code to find largest integer in a array
# version 1.0
def find_largest(numbers):
    array_read=numbers
    n=len(array_read)
    m=max(array_read)
    return m
print(find_largest([1, -28, 88, 200, 7]))  # 200
